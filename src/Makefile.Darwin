CC = gcc-7
CXX = g++
CFLAGS = -g -ffast-math
CXXFLAGS = -g
LDLIBS =
define ENV_SH
if [ -d "$(GSL)/lib" ] && [[ ":$$LD_LIBRARY_PATH:" != *":$(GSL)/lib:"* ]]; then
  export LD_LIBRARY_PATH="$${LD_LIBRARY_PATH:+"$$LD_LIBRARY_PATH:"}$(GSL)/lib"
fi
if [ -d "$(TACO)/lib" ] && [[ ":$$LD_LIBRARY_PATH:" != *":$(TACO)/lib:"* ]]; then
  export LD_LIBRARY_PATH="$${LD_LIBRARY_PATH:+"$$LD_LIBRARY_PATH:"}$(TACO)/lib"
fi
endef
